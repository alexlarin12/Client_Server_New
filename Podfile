project 'TryRequest.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TryRequest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod 'Alamofire'
  # Pods for TryRequest

  target 'TryRequestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TryRequestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
